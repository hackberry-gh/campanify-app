<%= I18n.with_locale(@resource.language) { 
	I18n.t('devise.mailer.reset_password_instructions.body', 
	email: @resource.email, 
	link: link_to( 
				t('devise.mailer.reset_password_instructions.link_title'), 
				edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
				) 
	)
} %>