<article class="form">
	
	<%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f| %>
	<%= devise_error_messages! %>
	
	<legend><%= t('html.users.confirmations.title') %></legend>

	<div>
		<%= f.label :email %>
		<%= f.email_field :email %>
	</div>

	<div><%= f.submit t('html.users.confirmations.submit') %></div>
	
	<% end %>

	<%= render "users/shared/links" %>
</article>