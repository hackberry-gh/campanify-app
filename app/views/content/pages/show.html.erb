<h1><%= page.title %></h1>
<article class="page" id="<%= page.slug %>">
<% render_content page %>
</article>
<aside class="widgets">
<% render_widgets page %>
</aside>
<%= render "shared/comments", :href => user_url(user) if Settings.pages['comments'].present? %>